# trend.yaml — DEFINITIONS 5절 스키마
# DEFINITIONS 1.3 이진 규칙: w_max if 조건 충족 else w_floor
rule:    "ma200"   # ma200 | tsmom12
w_floor: 0.0       # 방어 시 최소 노출 (기본 0.0, 변형 0.5는 M4 전용)
# w_max 는 base.yaml 에서 상속 (1.0)
